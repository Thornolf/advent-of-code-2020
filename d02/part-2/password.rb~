values = File.read('values.txt').split("\n")

values.reject! do |value|
  first, second, key, string = value.split /\s|-|,/
  matcher = 0
  matcher += 1 if string[first.to_i] == key
  matcher += 1 if string[second.to_i] == key

  true if != 1
end

p values.count
